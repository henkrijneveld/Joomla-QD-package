<?xml version="1.0" encoding="utf-8"?>
<extension type="component" version="3.2.0" method="upgrade">

	<name>Basic component!</name>
	<!-- The following elements are optional and free of formatting constraints -->
	<creationDate>May 2015</creationDate>
	<author>Henk Rijneveld</author>
	<authorEmail>henk@henkrijneveld.nl</authorEmail>
	<authorUrl>henks.website</authorUrl>
	<copyright>Whatever</copyright>
	<license>Whatever</license>
	<!--  The version string is recorded in the components table -->
	<version>0.0.1</version>
	<!-- The description is optional and defaults to the name -->
	<description>Description of the basiccomponent</description>

	<!-- Site Main File Copy Section -->
	<!-- Note the folder attribute: This attribute describes the folder
		to copy FROM in the package to install therefore files copied
		in this section are copied from /site/ in the package -->
	<files folder="site">
        <folder>views</folder>
		<filename>basiccomponent.php</filename>
	</files>

	<administration>
		<!-- Administration Menu Section -->
		<menu link='index.php?option=com_basiccomponent'>User importer</menu>
		<!-- Administration Main File Copy Section -->
		<!-- Note the folder attribute: This attribute describes the folder
			to copy FROM in the package to install therefore files copied
			in this section are copied from /admin/ in the package -->
		<files folder="admin">
			<!-- Admin Main File Copy Section -->
            <folder>vc</folder>
			<filename>basiccomponent.php</filename>
            <filename>config.xml</filename>
        </files>
	</administration>
</extension>
